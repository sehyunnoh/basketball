# Normalise line endings: the repo stores LF, checkouts get the platform's
# native endings. Keeps Windows work and the Linux CI build in agreement.
* text=auto eol=lf

# Binary assets git must not touch.
*.woff2 binary
*.png binary
